## Git

so we are not doing the code movement directly to main branch

git --> setting --> rule --> rulest --> main-branch(name) --> active --> Branch targeting criteria(main) --> require pull(checkbox) ---> create
from now we will not directly move the code to main branch

to create new branch
--------------------------
git checkout -b plain-dosa
 git add . && git commit -m "plain dosa and dosa batter added" && git push origin plain-dosa

main (8h9) -- (plain-dosa)(same code so same commi id 8h9 ) ---> ebf8(after adding dosa bettr)- commi from plain-dosa branch --> light oil commit (c8e)

now we need to push the changes for this we need raise a pull request
Raise PR 

comare: plain-dosa moving changes to main
create pull request
